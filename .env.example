# MongoDB connection string. For local development you can run MongoDB via
# Docker (`docker run -d -p 27017:27017 mongo:7`) or use a free MongoDB
# Atlas cluster (https://www.mongodb.com/atlas).
MONGODB_URI=mongodb://127.0.0.1:27017
MONGODB_DB=drivekit
